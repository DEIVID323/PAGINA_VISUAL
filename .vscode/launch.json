{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Abrir en Chrome",
      "file": "${file}",
      "webRoot": "${workspaceFolder}"
    }
  ]
}