{
    "version": "0.2.0",
    "configurations": [



        {
            "name": "Abrir con Live Server",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5500/${fileBasename}",
            "webRoot": "${workspaceFolder}"
        }
    ]
}