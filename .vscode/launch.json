{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Abrir proyecto",
            "url": "http://localhost:5500/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}